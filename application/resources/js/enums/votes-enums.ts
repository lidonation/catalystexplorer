export enum VoteEnum {
    YES = 1,
    NO = -1,
    ABSTAIN = 0,
}

export enum VisibilityEnum {
    PUBLIC = 'public',
    PRIVATE = 'private',
    DELEGATORS = 'delegators',
    UNLISTED = 'unlisted',
}

export enum StatusEnum {
    PUBLISHED = 'published',
    DRAFT = 'draft',
}
